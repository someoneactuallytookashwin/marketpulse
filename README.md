# MarketPulse

Starter repository structure for a data pipeline (Airflow + Python ingestion + dbt + BI dashboard).

## Local dev (placeholder)

1. Install dependencies
2. Start services (via `docker-compose.yml`)
3. Run ingestion + dbt models

More details can be added once you confirm the target sources and warehouse.

